import type { ConsolePaths } from "../../../types/console-paths.types.js";

export interface GenericDeviceContentTargetsFilePaths {
  "es-de-gamelists": {
    consoles: Partial<ConsolePaths>; // $CONTENT_TARGET_ES_DE_GAMELISTS_DIR_PATH/<console>/gamelist.xml
  };
}
