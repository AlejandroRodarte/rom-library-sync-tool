export interface ConsolePaths {
  dirs: {
    dbs: {
      roms: string;
      media: string;
      gamelists: string;
    };
  };
  files: {
    list: string;
    failed: string;
    diff: string;
  };
}
